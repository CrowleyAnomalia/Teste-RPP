//using System.Collections;
//using System.Collections.Generic;
//using UnityEngine;

//public class footstep : MonoBehaviour
//{
   // [SerializedField] AudioSource footstepsAudioSource = null;
   // [Header("Audio Clips")]
   // [SerializeField] AudioClip[] softSteps = null;
   // [SerializeField] AudioClip[] hardSteps = null;

   // [Header("Steps")]
     //   [SerialazeField] float timer = 0.5f;
   // private float stepTimer;
    
   // public void PlaySteps(GroundType groundType, float speedSpeednormalized)
   // {
    //    if (groundType == GroundType.None)
     //       return;
//
    //    stepsTimer += timer.fixedDeltaTime * speedSpeednormalized;
     //   if (stepsTimer >- timer)
     //   {
      //      var steps = groundType == GroundType.Hard? hardsteps : softsteps;
      //      int index = Random.Range(0, steps.Length);
        //    footstepsAudioSource.PlayOneShot(steps[index]);

        //    stepsTimer = 0;
      //  }
    //}
   // void Start()
   // {
        
   // }

   // Update is called once per frame
   // void Update()
   // {
        
   // }
//}
